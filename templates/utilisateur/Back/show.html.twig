{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}

    <div class="container pt-5">
        <h1>Mon profile</h1>
        <div class="pt-5">
            <div class="row">
                <div class="col-2 font-weight-bold">Nom</div>
                <div class="col-2 font-weight-bold">Prénom</div>
                <div class="col-2 font-weight-bold">Email</div>
            </div>
            <div class="row">
                <div class="col-2">{{ utilisateur.nom }}</div>
                <div class="col-2">{{ utilisateur.prenom }}</div>
                <div class="col-2">{{ utilisateur.email }}</div>
            </div>
        </div>
        <div class="col pt-5">
            <a href="{{ path('utilisateur_edit', {'id': utilisateur.id}) }}"class="btn btn-warning">Modifier</a>
            {{ include('utilisateur/Back/_delete_form.html.twig') }}
            <a href="{{ path('admin_utilisateur_index') }}" class="btn btn-primary">Retour à la liste</a>
        </div>
    </div>
    </div>


{% endblock %}
