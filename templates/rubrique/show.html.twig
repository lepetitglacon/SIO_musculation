{% extends 'base.html.twig' %}

{% block title %}Rubrique{% endblock %}

{% block body %}
    <h1>Rubrique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rubrique.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ rubrique.titre }}</td>
            </tr>
            <tr>
                <th>Dropdown</th>
                <td>{{ rubrique.dropdown ? 'Oui' : 'Non' }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex justify-content-around">
        <a href="{{ path('rubrique_index') }}" class="btn btn-primary">Retour à la liste</a>

        <a href="{{ path('rubrique_edit', {'id': rubrique.id}) }}" class="btn btn-warning">Modifier</a>

        {{ include('rubrique/_delete_form.html.twig') }}
    </div>

{% endblock %}
