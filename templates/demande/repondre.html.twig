{% extends 'base.html.twig' %}

{% block title %}Repondre à la demande{% endblock %}

{% block body %}
    <div class="container">
        <h1>Répondre à la demande #{{ demande.id }}</h1>
        <div class="d-flex flex-column">
            <label>
    <textarea type="text" placeholder="{{ demande.objet }}"></textarea>
    <textarea type="text" placeholder="{{ demande.texte }}"></textarea>
            </label></div>
        <hr>
        <div>
            {{ include('demande/_form.html.twig') }}
        </div>

    </div>

{% endblock %}
