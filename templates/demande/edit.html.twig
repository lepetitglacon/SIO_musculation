{% extends 'base.html.twig' %}

{% block title %}Edit Demande{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier la demande</h1>

        {{ include('demande/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('demande_index') }}" class="btn btn-secondary">Retour à la liste</a>

        {{ include('demande/_delete_form.html.twig') }}
    </div>

{% endblock %}
