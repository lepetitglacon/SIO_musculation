{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <div class="container">
        <h1>Article</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ article.texte }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ article.dateCreation ? article.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
            </tbody>
        </table>
        <div class="col d-flex">
            <a href="{{ path('admin_article_index') }}" class="btn btn-primary mr-2">Retour à la liste</a>

            <a href="{{ path('admin_article_edit', {'id': article.id}) }}" class="btn btn-warning mr-2">Modifier</a>

            {{ include('article/Back/_delete_form.html.twig') }}
        </div>
    </div>


{% endblock %}
